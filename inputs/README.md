# inputs

仕様の生情報（議事録・チャット・クライアント資料など）の入口。`/digest-inputs` でここを消化して `docs/` を更新する。

- `meetings/` — 議事録
- `chats/` — Slack / Teams 等のエクスポート
- `files/` — クライアント送付ドキュメント

> サブディレクトリは初回利用時に作成してよい。消化済みファイルもエビデンスとして残す。
